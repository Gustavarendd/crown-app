import Directory from '../../components/directory/directory.component';

function Home() {
  return (
    <div
      style={{
        display: 'flex',
        justifyContent: 'center',
      }}
    >
      <Directory />
    </div>
  );
}

export default Home;
